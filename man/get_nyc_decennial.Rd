% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_nyc_decennial.R
\name{get_nyc_decennial}
\alias{get_nyc_decennial}
\title{Get Decennial Census data for New York City}
\usage{
get_nyc_decennial(
  variables,
  year = 2020,
  geography = c("tract", "block", "block group"),
  sumfile = "pl",
  summary_var = NULL,
  output = c("wide", "tidy"),
  ...
)
}
\arguments{
\item{variables}{A character vector (optionally named) of Census variable
IDs. See \code{\link[=load_nyc_variables]{load_nyc_variables()}} or \code{\link[tidycensus:load_variables]{tidycensus::load_variables()}}.}

\item{year}{Decennial Census year. Defaults to \code{2020}. \code{2000} and \code{2010}
are also accepted by \code{tidycensus}.}

\item{geography}{Geographic level. One of \code{"tract"} (default), \code{"block"},
or \code{"block group"}.}

\item{sumfile}{Census summary file. Defaults to \code{"pl"} (the redistricting
file). For tract-level demographic and housing characteristics use
\code{"dhc"}.}

\item{summary_var}{Optional summary (denominator) variable.}

\item{output}{\code{"wide"} (the default) or \code{"tidy"}.}

\item{...}{Additional arguments forwarded to \code{\link[tidycensus:get_decennial]{tidycensus::get_decennial()}}.}
}
\value{
A tibble with \code{geoid} and \code{county} as the first two columns,
followed by the requested variables. The Census year and sumfile used
are stored in the \code{"decennial_year"} and \code{"decennial_sumfile"}
attributes.
}
\description{
A thin wrapper around \code{\link[tidycensus:get_decennial]{tidycensus::get_decennial()}} that hard-codes the
five New York City counties, never downloads geometry, and returns a tidy
tibble keyed by \code{geoid} (lowercased) with a \code{county} column ready for
joining to the spatial objects in the \code{nycmaps} package.
}
\examples{
\dontrun{
# 2020 PL 94-171 race and Hispanic origin at the block level.
race_vars <- c(
  nh_white = "P2_005N",
  nh_black = "P2_006N",
  nh_asian = "P2_008N",
  hispanic = "P2_002N"
)
get_nyc_decennial(
  race_vars,
  geography = "block",
  sumfile = "pl",
  summary_var = "P1_001N"
)

# 2020 DHC household type at tract level.
get_nyc_decennial(
  c(family_hh = "H9_002N", nonfamily_hh = "H9_009N"),
  geography = "tract",
  sumfile = "dhc"
)
}

}
\seealso{
\code{\link[=get_nyc_acs]{get_nyc_acs()}}, \code{\link[=load_nyc_variables]{load_nyc_variables()}}, \code{\link[=nyc_counties]{nyc_counties()}}
}
