% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_nyc_variables.R
\name{load_nyc_variables}
\alias{load_nyc_variables}
\title{Look up Census variables}
\usage{
load_nyc_variables(year, dataset, cache = TRUE)
}
\arguments{
\item{year}{The year, or endyear, of the dataset.}

\item{dataset}{Dataset name. Common choices: \code{"acs5"}, \code{"pl"}, \code{"dhc"},
\code{"sf1"}. See \code{\link[tidycensus:load_variables]{tidycensus::load_variables()}} for the full list.}

\item{cache}{Whether to cache the variable table on disk for re-use across
sessions. Defaults to \code{TRUE} (the opposite of the upstream default,
because caching is almost always what you want during package work).}
}
\value{
A tibble of variable IDs, labels, and concepts.
}
\description{
Convenience wrapper around \code{\link[tidycensus:load_variables]{tidycensus::load_variables()}} for browsing the
variable catalogues used by \code{\link[=get_nyc_acs]{get_nyc_acs()}} and \code{\link[=get_nyc_decennial]{get_nyc_decennial()}}. The
result is identical to a direct \code{tidycensus} call; this function exists so
that all NYC-oriented Census helpers live in one place.
}
\examples{
\dontrun{
acs <- load_nyc_variables(2023, "acs5")
dplyr::filter(acs, grepl("median household income", label, ignore.case = TRUE))
}

}
